import {IGoogleUser, getUsername, generatePassword, createGoogleUser} from '../google.auth';

describe.skip('Google auth helper functions:', () => {
    test('get username from email', () => {
    });

    test('generate random password', () => {
    });

    test('create user from email', () => {
    });
});
