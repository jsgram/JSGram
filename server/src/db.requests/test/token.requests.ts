import {isTokenExist, deleteToken} from '../token.requests';

describe.skip('Token CRUD:', () => {
    test('token existence check fails', () => {
    });

    test('token existence check succeeds', () => {
    });

    test('token deletion fails', () => {
    });

    test('token deletion succeeds', () => {
    });
});
