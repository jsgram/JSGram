import {confirmUserRouter} from '../confirm.user.router';

describe.skip('Confirm user routes:', () => {
    test('confirmation handled successfully', () => {
    });

    test('resend handled successfully', () => {
    });

    test('unknown route returns error', () => {
    });
});
