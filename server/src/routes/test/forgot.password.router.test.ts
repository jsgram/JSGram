import {forgotPassword} from '../forgot.password';

describe.skip('Forgot password routes:', () => {
    test('reset password handled successfully', () => {
    });

    test('check email handled successfully', () => {
    });

    test('update password handled successfully', () => {
    });

    test('unknown route returns error', () => {
    });
});
