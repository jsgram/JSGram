import {googleRouter} from '../google.router';

describe.skip('Authentication with Google routes:', () => {
    test('google authenentication handled successfully', () => {
    });

    test('unknown route returns error', () => {
    });
});
