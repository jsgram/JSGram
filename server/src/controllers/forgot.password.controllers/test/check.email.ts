import {checkEmail} from '../check.email';

describe.skip('Check user email controller:', () => {
    test('email found succesfully', () => {
    });

    test('generic error while checking user email', () => {
    });
});
