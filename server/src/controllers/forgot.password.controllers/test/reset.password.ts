import {resetPassword} from '../reset.password';

describe.skip('Reset password controller:', () => {
    test('reset password redirected successfully', () => {
    });

    test('generic error while trying to reset password', () => {
    });
});
