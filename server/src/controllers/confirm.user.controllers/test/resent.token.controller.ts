import {resend} from '../resend.token.controller';

describe.skip('Resend token controller:', () => {
    test('token resend handled successfully', () => {
    });

    test('generic error while attempting to resend token', () => {
    });
});
