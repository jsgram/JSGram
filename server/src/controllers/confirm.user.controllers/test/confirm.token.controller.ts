import {confirm} from '../confirm.token.controller';

describe.skip('Confirm token controller:', () => {
    test('token confirmation redirected successfully', () => {
    });

    test('generic error while attempting token confirmation', () => {
    });
});
