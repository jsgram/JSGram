import {createUser, create} '../create.user';

describe.skip('Create user CRUD controller:', () => {
    test('user created successfully', () => {
    });

    test('user creation errored because of invalid email', () => {
    });

    test('user created successfully and verification email sent', () => {
    });
});
