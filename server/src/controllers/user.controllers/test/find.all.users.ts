import {findAll} from '../find.all.users';

describe.skip('Bulk user selection controller:', () => {
    test('users selected successfully', () => {
    });

    test('generic error while finding users', () => {
    });
});
