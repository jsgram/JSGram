version: v1.0
name: JSgram CI
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: Test
    task:
      jobs:
        - name: TSlint
          commands:
            - npm install
            - npm test
